pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("libs.version.toml"))
        }
    }
}

rootProject.name = 'matching-engine'